import React from 'react';
import ReactDOM from 'react-dom';

import {AppRouteTarget as Routes} from './module/framework/app/AppRouteTarget';
// import App from './App';
// import * as serviceWorker from './serviceWorker';

export default Routes;

/*

ReactDOM.render(
  <React.StrictMode>
    <App applogo={applogo} clientlogo={clientlogo}
          apptitle={config.APPTITLE} appversion={config.VERSION}
          appsubtitle={config.APPSUBTITLE} appclient={config.APPCLIENT}/>
  </React.StrictMode>,
  document.getElementById('root')
);
*/

// If you want your app to work offline and load faster, you can change
// unregister() to register() below. Note this comes with some pitfalls.
// Learn more about service workers: https://bit.ly/CRA-PWA
// serviceWorker.unregister();
